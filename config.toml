# The URL the site will be built for
base_url = "https://peguerin.io"

title = "PEGUERIN"
description = "A clean born to work bioinformatics blog"

# The default language, used in RSS
default_language = "en"


# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true


# The taxonomies to be rendered for that site and their configuration
# Example:
#     taxonomies = [
#       {name = "tags", rss = true}, # each tag will have its own RSS feed
#       {name = "categories", paginate_by = 5},  # 5 items per page for a term
#       {name = "authors"}, # Basic definition: no RSS or pagination
#     ]
#
taxonomies = []
# The additional languages for that site
# Example:
#     languages = [
#       {code = "fr", rss = true}, # there will be a RSS feed for French content
#       {code = "it"}, # there won't be a RSS feed for Italian content
#     ]
#
languages = [
    {code = "fr"}, 
    {code = "es"}, # there won't be a RSS feed for Spannish content
    {code = "en", rss = true}, # there will be a RSS feed for English content
]




[extra]
# Put all your custom variables here





links = [
    {url = "https://gitlab.mbb.univ-montp2.fr/users/peguerin/groups", name = "About"},
]